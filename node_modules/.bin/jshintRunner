#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jshintrunner/bin/jshintRunner" "$@"
  ret=$?
else 
  node  "$basedir/../jshintrunner/bin/jshintRunner" "$@"
  ret=$?
fi
exit $ret
